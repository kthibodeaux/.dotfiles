#!/bin/sh
if [ $# -eq 0 ]; then
  content=$(cat)
else
  content=$(cat $1)
fi

curl -X POST -s -d "$content" http://10.0.1.42/documents | echo "http://10.0.1.42/"`ruby -e "puts ARGF.read.to_s.match(/\{\"key\":\"(.+)\"\}/).captures.first"` | pbcopy
